# package_version

NACC ADRC data release date

## Usage

``` r
data(package_version)
```

## Format

A character object representing the NACCADRC R data package version. The
format of the version is \`uds_version.data_release_date.minor_update
